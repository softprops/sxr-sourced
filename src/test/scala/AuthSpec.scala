package implicitly

import org.specs._

object AuthSpec extends Specification {
  "Auth" should {
    "authorize a valid org" in {
      
    }
    "not authorize an invalid org" in {
      
    }
  }
}