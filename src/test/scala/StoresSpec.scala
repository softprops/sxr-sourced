package implicitly

import org.specs._

object StoresSpec extends Specification {
  "Stores" should {
    "store a and provide access to an sxr document" in {
      
    }
    "store and provide access to valid org secrete" in {
      
    }
  }
}